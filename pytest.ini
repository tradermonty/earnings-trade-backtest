[pytest]
addopts = -m "not live_api and not slow and not legacy"
testpaths = tests
markers =
    live_api: tests that call external APIs and require credentials/network
    slow: long-running tests that are not part of the default feedback loop
    legacy: script-style or obsolete tests kept for reference during migration
